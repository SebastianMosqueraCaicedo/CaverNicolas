class Interfaz extends Ent {
    constructor(x, y) {
        super(x, y);
    }

draw(){
    switch (this.estado) {
        case 9:
            
            break;
    
        default:
            break;
    }
}

    contar(jugador){

    }
}